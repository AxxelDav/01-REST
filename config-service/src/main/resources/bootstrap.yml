server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          default-label: "3-eureka-server"
          uri: https://github.com/AxxelDav/ms-store.git
          searchPaths: config-data
  security:
    user:
      name: root
      password: s3cr3t