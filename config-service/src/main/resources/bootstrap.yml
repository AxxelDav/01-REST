server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          default-label: "7-admin-actuator"
          uri: https://github.com/AxxelDav/ms-store.git
          searchPaths: config-data
  security:
    user:
      name: root
      password: s3cr3t