server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          default-label: "8-sleuth"
          uri: https://github.com/AxxelDav/ms-store.git
          searchPaths: config-data
  security:
    user:
      name: root
      password: s3cr3t
